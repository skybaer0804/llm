💡 맥미니에서 추천하는 설치 순서
가장 대중적이고 가벼운 venv 모듈을 사용하는 방법입니다.
폴더 생성 및 이동:
bash
mkdir my-autogen-project && cd my-autogen-project
코드를 사용할 때는 주의가 필요합니다.

가상 환경 생성:
bash
python3 -m venv .venv
코드를 사용할 때는 주의가 필요합니다.

가상 환경 활성화:
bash
source .venv/bin/activate
코드를 사용할 때는 주의가 필요합니다.

AutoGen 설치:
bash
pip install pyautogen
코드를 사용할 때는 주의가 필요합니다.