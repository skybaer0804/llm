# LLM 라우터 모니터링

## 🔍 모니터링 엔드포인트

```bash
# 헬스 체크
curl http://localhost:8000/health

# 메모리 현황
curl http://localhost:8000/memory

# 등록된 모델 + 라우팅 규칙
curl http://localhost:8000/models
```

이 라우터를 AutoGen의 `llm_config`에 연결하면,  
설계 → 구현 → 검수의 **완전 자동화 파이프라인**이 완성됩니다! 🚀